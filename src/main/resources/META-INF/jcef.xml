<!-- Loaded only when the bundled JCEF plugin is present (262). Deliberately empty: the dependency itself is what
     puts JCEF's classloader among ours so the report tab can reach JBCefBrowser. -->
<idea-plugin/>
