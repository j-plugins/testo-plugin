<!-- Loaded only when the bundled JCEF plugin (com.intellij.modules.jcef) is present, which is how 262 ships it.
     Deliberately empty of extensions: the point is the dependency itself, which puts JCEF's classloader among ours so
     the report tab can reach JBCefBrowser. The report FileEditorProvider stays in the main descriptor — it mentions no
     JCEF type, and its editor is only ever created once TestoReportViewer.isAvailable has said yes. -->
<idea-plugin/>
